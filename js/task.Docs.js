"use strict";
/* 
•Գրեք պատասխանը: let x = 5; alert( x++ );  // 5 
•Գրեք պատասխանը: [ ] + false - null + true  // NaN     
•Գրեք պատասխանը: let y = 1; let x = y = 2; alert(x); // 2
•Գրեք պատասխանը։ [ ] + 1 + 2  // 12   
•Գրեք պատասխանը: alert( "1"[0] )  // 1 
•Գրեք պատասխանը։ 2 && 1 && null && 0 && undefined // null  
•Կա՞ ասյտեղ տարբերություն։ !!( a && b ) ու (a && b)  // ոչ 
•Գրեք պատասխանը: alert( null || 2 && 3 || 4 ); // 3  
• a = [1, 2, 3]; b = [1, 2, 3];  Ճի՞շտ է արդյոք, որ a == b // false
•Գրեք պատասխանը: alert( +"Infinity" ); // Infinity
•Գրեք պատասխանը: "Lucas" > "armstrong"  // false 
•Գրեք պատասխանը: 0 || "" || 2 || undefined || true || falsе // 2
*/ 